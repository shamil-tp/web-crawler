<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>N-jin Search</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>
        :root {
    --bg-color: #f6f9ff;
    --primary-gradient: linear-gradient(135deg, #3b82f6, #6366f1, #8b5cf6);
    --primary-blue: #4f46e5;
    --title-visited: #7c3aed;
    --text-main: #4b5563;
    --text-dark: #111827;
    --border-light: #e5e7eb;
}


        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-color);
            color: var(--text-dark);
            -webkit-font-smoothing: antialiased;
        }
        body {
    background: radial-gradient(circle at top left, #eef2ff, #f8fafc);
}


        /* --- THE LOGO --- */
        .njin-logo {
            font-size: 5.5rem;
            font-weight: 800;
            background: linear-gradient(45deg, #4285f4, #ea4335, #fbbc05, #34a853);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -3px;
            user-select: none;
        }

        .njin-logo-small {
            font-size: 2.2rem;
            font-weight: 800;
            background: linear-gradient(45deg, #4285f4, #ea4335, #fbbc05, #34a853);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -1.5px;
            text-decoration: none;
            padding-right: 20px;
        }

        /* --- SEARCH BAR EFFECTS --- */
        .search-container {
    background: #ffffff;
    border-radius: 50px;
    border: 1px solid #e5e7eb;
    transition: all 0.3s ease;
    box-shadow: 0 5px 20px rgba(99,102,241,0.08);
}

.search-container:hover,
.search-container:focus-within {
    box-shadow: 0 8px 30px rgba(99,102,241,0.18);
    transform: translateY(-2px);
}


        .form-control:focus {
            box-shadow: none;
        }

        .search-input {
            font-size: 1.1rem;
            color: var(--text-dark);
        }

        /* --- SEARCH RESULTS LIST (FLAT DESIGN) --- */
        .results-wrapper {
            max-width: 700px; /* Optimal reading width for text */
            margin-left: max(100px, 5%);
        }
        .njin-btn {
    background: var(--primary-gradient);
    border: none;
    color: white;
    font-weight: 600;
    padding: 12px 22px;
    border-radius: 50px;
    transition: all 0.3s ease;
    transform: translateX(-9%);
}

.njin-btn:hover {
    transform: scale(1.05);
    transform: translateX(-9%);
    box-shadow: 0 6px 20px rgba(99,102,241,0.4);
}


        @media (max-width: 768px) {
            .results-wrapper { margin-left: auto; margin-right: auto; }
        }

        .result-item {
            margin-bottom: 2.5rem; /* Generous spacing instead of borders */
        }

        .result-url-container {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 4px;
        }

        .favicon-img {
            width: 18px;
            height: 18px;
            border-radius: 50%;
            background-color: #f1f3f4;
            padding: 2px;
        }

        .result-url {
            color: var(--text-dark);
            font-size: 0.875rem;
            text-decoration: none;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            max-width: 100%;
        }

        .result-title {
    background: var(--primary-gradient);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    font-size: 1.25rem;
    font-weight: 600;
    text-decoration: none;
}


        .result-title:visited {
            color: var(--title-visited);
        }

        .result-title:hover {
            text-decoration: underline;
        }

        .result-desc {
            color: var(--text-main);
            font-size: 0.875rem;
            line-height: 1.58;
            word-wrap: break-word;
        }

        /* --- MODERN PAGINATION --- */
        .njin-pagination {
            display: flex;
            gap: 8px;
            padding-top: 20px;
        }

        .page-btn {
            color: var(--primary-blue);
            background: none;
            border: none;
            padding: 8px 16px;
            border-radius: 100px;
            text-decoration: none;
            font-weight: 500;
            font-size: 0.9rem;
            transition: background-color 0.2s;
        }

        .page-btn:hover {
            background-color: #f1f3f4;
            color: var(--primary-blue);
        }

        .page-indicator {
            padding: 8px 16px;
            color: var(--text-dark);
            font-weight: 500;
            font-size: 0.9rem;
        }
    </style>
</head>

<body>

    <% if (!results) { %>
        <div class="container d-flex flex-column justify-content-center align-items-center vh-100">
            <!-- <h1 class="njin-logo mb-4">N-jin</h1> -->
             <img src="/logo/logo.png" alt="">
             <p class="text-muted mt-3" style="font-weight:500;">
    Discover the web differently.
</p>

            <form action="/search" method="GET" class="w-100 px-3" style="max-width: 620px;">
    <div class="search-container d-flex align-items-center">
        
        <i class="bi bi-search text-muted ms-3 me-2"></i>

        <input type="text" name="q"
            class="form-control border-0 search-input py-3 shadow-none"
            placeholder="Search the web"
            required autofocus autocomplete="off">

        <button type="submit" class="njin-btn">
            Search
        </button>

    </div>
</form>

        </div>
    <% } else { %>

    <div class="container-fluid py-3 border-bottom sticky-top" style="background-color: var(--bg-color);">
            <div class="d-flex align-items-center px-md-4 flex-wrap gap-3">
                <a href="/" class="njin-logo-small"><img src="/logo/logo.png" alt="" width="70px"></a>
                
                <form action="/search" method="GET" style="flex: 1; max-width: 650px;">
                    <div class="search-container d-flex align-items-center px-3">
                        <input type="text" name="q" class="form-control border-0 search-input py-2 shadow-none" value="<%= query %>" required autocomplete="off">
                        <button class="btn border-0 text-primary" type="submit">
                            <i class="bi bi-search" style="font-size: 1.1rem;"></i>
                        </button>
                    </div>
                </form>
            </div>
        </div>

        <div class="container-fluid mt-4 mb-5">
            <div class="results-wrapper">
                
                <p class="text-muted small mb-4 pb-2 border-bottom" style="color: #70757a !important;">
                    About <%= totalResults.toLocaleString() %> results
                </p>

                <% if (results.length === 0) { %>
                    <div class="mt-4">
                        <p class="mb-3" style="font-size: 1.1rem;">Your search - <strong><%= query %></strong> - did not match any documents.</p>
                        <p class="mb-2">Suggestions:</p>
                        <ul class="text-muted" style="line-height: 1.8;">
                            <li>Make sure all words are spelled correctly.</li>
                            <li>Try different or more general keywords.</li>
                            <li>Give the N-jin crawler more time to index the web.</li>
                        </ul>
                    </div>
                <% } else { %>

                    <% results.forEach(site => { %>
                        <div class="result-item">
                            
                            <div class="result-url-container">
                                <% if (site.favicon) { %>
                                    <img src="<%= site.favicon %>" alt="" class="favicon-img" onerror="this.style.display='none'">
                                <% } else { %>
                                    <div class="favicon-img d-flex align-items-center justify-content-center">
                                        <i class="bi bi-globe2" style="font-size: 10px; color: #70757a;"></i>
                                    </div>
                                <% } %>
                                <a href="<%= site.url %>" class="result-url">
                                    <%= site.url.replace(/^https?:\/\//, '') %>
                                </a>
                            </div>
                            
                            <a href="<%= site.url %>" class="result-title">
                                <%= site.title %>
                            </a>
                            
                            <p class="result-desc mb-0">
                                <% if (site.description) { %>
                                    <%= site.description %>
                                <% } else { %>
                                    <%= site.content.substring(0, 160) %>...
                                <% } %>
                            </p>
                        </div>
                    <% }) %>

                    <div class="njin-pagination mt-5 pt-3 border-top">
                        <% if (currentPage > 1) { %>
                            <a href="/search?q=<%= encodeURIComponent(query) %>&page=<%= currentPage - 1 %>" class="page-btn">
                                <i class="bi bi-chevron-left me-1"></i> Previous
                            </a>
                        <% } %>
                        
                        <span class="page-indicator">Page <%= currentPage %> of <%= totalPages %></span>

                        <% if (currentPage < totalPages) { %>
                            <a href="/search?q=<%= encodeURIComponent(query) %>&page=<%= currentPage + 1 %>" class="page-btn">
                                Next <i class="bi bi-chevron-right ms-1"></i>
                            </a>
                        <% } %>
                    </div>

                <% } %> 
            </div>
        </div>

    <% } %> 

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>N-jin Search</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Outfit:wght@600;700;800&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>
        :root {
            --bg-dark: #0f0f14;
            --bg-card: #1a1a24;
            --bg-elevated: #222230;
            --accent-primary: #6366f1;
            --accent-secondary: #8b5cf6;
            --text-primary: #e4e4e7;
            --text-secondary: #a1a1aa;
            --text-muted: #71717a;
            --border-subtle: rgba(255, 255, 255, 0.06);
            --border-light: rgba(255, 255, 255, 0.1);
            --glow-subtle: rgba(99, 102, 241, 0.08);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--bg-dark);
            color: var(--text-primary);
            overflow-x: hidden;
            position: relative;
            line-height: 1.6;
        }

        /* Subtle animated background */
        body::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                radial-gradient(circle at 20% 30%, rgba(99, 102, 241, 0.03), transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(139, 92, 246, 0.03), transparent 50%);
            pointer-events: none;
            z-index: 0;
        }

        /* Content wrapper */
        .content-wrapper {
            position: relative;
            z-index: 1;
        }

        /* Logo styling */
        .njin-logo {
            font-family: 'Outfit', sans-serif;
            font-size: clamp(3.5rem, 10vw, 6rem);
            font-weight: 800;
            letter-spacing: -3px;
            background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            user-select: none;
            position: relative;
            display: inline-block;
        }

        .njin-logo-small {
            font-family: 'Outfit', sans-serif;
            font-size: clamp(1.3rem, 4vw, 1.8rem);
            font-weight: 800;
            letter-spacing: -1px;
            background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-decoration: none;
            transition: opacity 0.3s ease;
        }

        .njin-logo-small:hover {
            opacity: 0.8;
        }

        /* Tagline */
        .tagline {
            font-size: clamp(0.9rem, 2.5vw, 1.1rem);
            color: var(--text-secondary);
            font-weight: 500;
            letter-spacing: 1px;
            margin-top: 1rem;
            opacity: 0;
            animation: fadeInUp 0.6s ease forwards;
            animation-delay: 0.2s;
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(15px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Search container */
        .search-container {
            background: var(--bg-card);
            border: 1px solid var(--border-subtle);
            border-radius: 16px;
            padding: 6px 6px 6px 20px;
            transition: all 0.3s ease;
            position: relative;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2);
        }

        .search-container:hover,
        .search-container:focus-within {
            border-color: var(--border-light);
            box-shadow: 
                0 8px 30px rgba(0, 0, 0, 0.3),
                0 0 0 3px var(--glow-subtle);
        }

        .search-input {
            background: transparent;
            border: none;
            color: var(--text-primary);
            font-size: clamp(0.95rem, 2.5vw, 1.05rem);
            font-weight: 500;
            padding: 14px 0;
            width: 100%;
        }

        .search-input::placeholder {
            color: var(--text-muted);
            font-weight: 400;
        }

        .search-input:focus {
            outline: none;
            box-shadow: none;
        }

        .search-icon {
            color: var(--text-muted);
            font-size: 1.1rem;
            transition: color 0.3s ease;
        }

        .search-container:focus-within .search-icon {
            color: var(--accent-primary);
        }

        /* Search button */
        .njin-btn {
            background: linear-gradient(135deg, var(--accent-primary), var(--accent-secondary));
            border: none;
            color: white;
            font-weight: 600;
            padding: 13px 28px;
            border-radius: 12px;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
            font-size: clamp(0.85rem, 2vw, 0.95rem);
            white-space: nowrap;
        }

        .njin-btn:hover {
            transform: translateY(-1px);
            box-shadow: 0 8px 24px rgba(99, 102, 241, 0.3);
        }

        .njin-btn:active {
            transform: translateY(0);
        }

        /* Header/Navbar */
        .search-header {
            background: rgba(15, 15, 20, 0.85);
            backdrop-filter: blur(12px);
            border-bottom: 1px solid var(--border-subtle);
            position: sticky;
            top: 0;
            z-index: 100;
        }

        /* Results wrapper */
        .results-wrapper {
            max-width: 720px;
            margin: 0 auto;
            padding: 0 1rem;
        }

        @media (min-width: 992px) {
            .results-wrapper {
                margin-left: max(100px, 10%);
                margin-right: auto;
            }
        }

        /* Result items */
        .result-item {
            margin-bottom: 2rem;
            padding: 1.25rem;
            background: var(--bg-card);
            border-radius: 12px;
            border: 1px solid var(--border-subtle);
            transition: all 0.25s ease;
            position: relative;
        }

        .result-item:hover {
            background: var(--bg-elevated);
            border-color: var(--border-light);
            transform: translateX(4px);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
        }

        /* URL container */
        .result-url-container {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 6px;
            flex-wrap: wrap;
        }

        .favicon-img {
            width: 18px;
            height: 18px;
            border-radius: 4px;
            background: rgba(255, 255, 255, 0.03);
            padding: 2px;
            flex-shrink: 0;
        }

        .result-url {
            color: var(--text-muted);
            font-size: 0.8rem;
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s ease;
            word-break: break-all;
        }

        .result-url:hover {
            color: var(--accent-primary);
        }

        /* Result title */
        .result-title {
            color: var(--text-primary);
            font-size: clamp(1.1rem, 3vw, 1.25rem);
            font-weight: 600;
            text-decoration: none;
            display: block;
            margin-bottom: 0.5rem;
            transition: color 0.25s ease;
            line-height: 1.4;
        }

        .result-title:hover {
            color: var(--accent-primary);
        }

        .result-title:visited {
            color: var(--text-secondary);
        }

        /* Result description */
        .result-desc {
            color: var(--text-secondary);
            font-size: clamp(0.85rem, 2vw, 0.92rem);
            line-height: 1.65;
            margin-bottom: 0;
        }

        /* Stats bar */
        .stats-bar {
            color: var(--text-muted);
            font-size: 0.82rem;
            font-weight: 500;
            padding: 0.75rem 0;
            border-bottom: 1px solid var(--border-subtle);
            margin-bottom: 1.5rem;
        }

        /* Pagination */
        .njin-pagination {
            display: flex;
            gap: 10px;
            padding: 2rem 0;
            align-items: center;
            flex-wrap: wrap;
            justify-content: center;
        }

        @media (min-width: 768px) {
            .njin-pagination {
                justify-content: flex-start;
            }
        }

        .page-btn {
            background: var(--bg-card);
            border: 1px solid var(--border-subtle);
            color: var(--text-primary);
            padding: 10px 18px;
            border-radius: 10px;
            text-decoration: none;
            font-weight: 600;
            font-size: 0.88rem;
            transition: all 0.25s ease;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .page-btn:hover {
            background: var(--bg-elevated);
            border-color: var(--accent-primary);
            color: var(--accent-primary);
            transform: translateY(-1px);
        }

        .page-indicator {
            color: var(--text-secondary);
            font-weight: 600;
            font-size: 0.88rem;
            padding: 0 10px;
        }

        /* No results styling */
        .no-results {
            background: var(--bg-card);
            border: 1px solid var(--border-subtle);
            border-radius: 12px;
            padding: 2.5rem 2rem;
            margin-top: 2rem;
        }

        .no-results h2 {
            color: var(--text-primary);
            font-size: clamp(1.2rem, 3vw, 1.4rem);
            font-weight: 600;
            margin-bottom: 1rem;
        }

        .no-results ul {
            max-width: 500px;
            margin: 1rem 0;
            color: var(--text-secondary);
            padding-left: 1.5rem;
        }

        .no-results li {
            margin-bottom: 0.5rem;
            line-height: 1.7;
        }

        /* Animations on load */
        .result-item {
            opacity: 0;
            animation: resultFadeIn 0.4s ease forwards;
        }

        .result-item:nth-child(1) { animation-delay: 0.05s; }
        .result-item:nth-child(2) { animation-delay: 0.1s; }
        .result-item:nth-child(3) { animation-delay: 0.15s; }
        .result-item:nth-child(4) { animation-delay: 0.2s; }
        .result-item:nth-child(5) { animation-delay: 0.25s; }
        .result-item:nth-child(n+6) { animation-delay: 0.3s; }

        @keyframes resultFadeIn {
            from {
                opacity: 0;
                transform: translateY(10px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* Home page container */
        .home-container {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            padding: 2rem 1rem;
        }

        /* Improved focus states for accessibility */
        .search-input:focus,
        .njin-btn:focus,
        .page-btn:focus,
        .result-title:focus {
            outline: 2px solid var(--accent-primary);
            outline-offset: 2px;
        }

        /* Loading state placeholder */
        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.5; }
        }

        /* Responsive typography adjustments */
        @media (max-width: 768px) {
            .njin-logo {
                letter-spacing: -2px;
            }

            .search-container {
                padding: 6px 6px 6px 16px;
            }

            .result-item {
                padding: 1rem;
            }

            .njin-btn {
                padding: 12px 20px;
            }
        }

        @media (max-width: 480px) {
            .tagline {
                letter-spacing: 0.5px;
            }

            .njin-btn {
                padding: 11px 18px;
                font-size: 0.85rem;
            }

            .result-item {
                margin-bottom: 1.5rem;
            }
        }

        /* Better touch targets for mobile */
        @media (hover: none) and (pointer: coarse) {
            .page-btn,
            .result-title,
            .result-url {
                min-height: 44px;
                display: flex;
                align-items: center;
            }
        }
    </style>
</head>

<body>
    <div class="content-wrapper">
        <% if (!results) { %>
            <!-- Home page -->
            <div class="home-container">
                <% if (typeof logoPath !== 'undefined' && logoPath) { %>
                    <img src="<%= logoPath %>" alt="N-jin" style="max-width: min(200px, 60vw); margin-bottom: 1.5rem;">
                <% } else { %>
                    <h1 class="njin-logo">N-jin</h1>
                <% } %>
                
                <p class="tagline">Search smarter, find faster</p>

                <form action="/search" method="GET" class="w-100 px-3 mt-5" style="max-width: 650px;">
                    <div class="search-container d-flex align-items-center">
                        <i class="bi bi-search search-icon me-3"></i>
                        <input 
                            type="text" 
                            name="q"
                            class="search-input"
                            placeholder="Search the web..."
                            required 
                            autofocus 
                            autocomplete="off"
                            aria-label="Search query">
                        <button type="submit" class="njin-btn ms-2" aria-label="Search">
                            Search
                        </button>
                    </div>
                </form>
            </div>

        <% } else { %>
            <!-- Results page -->
            <div class="search-header py-3">
                <div class="container-fluid px-3 px-md-4">
                    <div class="d-flex align-items-center flex-wrap gap-3">
                        <% if (typeof logoPath !== 'undefined' && logoPath) { %>
                            <a href="/" aria-label="Go to homepage">
                                <img src="<%= logoPath %>" alt="N-jin" style="width: clamp(50px, 10vw, 60px);">
                            </a>
                        <% } else { %>
                            <a href="/" class="njin-logo-small" aria-label="Go to homepage">N-jin</a>
                        <% } %>
                        
                        <form action="/search" method="GET" style="flex: 1; max-width: 600px; min-width: 0;">
                            <div class="search-container d-flex align-items-center">
                                <i class="bi bi-search search-icon me-2"></i>
                                <input 
                                    type="text" 
                                    name="q" 
                                    class="search-input" 
                                    value="<%= query %>" 
                                    required 
                                    autocomplete="off"
                                    aria-label="Search query">
                                <button class="btn border-0 p-2" type="submit" style="color: var(--accent-primary);" aria-label="Search">
                                    <i class="bi bi-search" style="font-size: 1.3rem;"></i>
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>

            <div class="container-fluid mt-4 mb-5">
                <div class="results-wrapper">
                    
                    <p class="stats-bar">
                        About <%= totalResults.toLocaleString() %> results
                    </p>

                    <% if (results.length === 0) { %>
                        <div class="no-results">
                            <h2>No results found for "<%= query %>"</h2>
                            <p style="color: var(--text-secondary); margin-bottom: 1.5rem;">Try these suggestions to improve your search:</p>
                            <ul>
                                <li>Check your spelling and try again</li>
                                <li>Use more general or different keywords</li>
                                <li>Try fewer keywords for broader results</li>
                                <li>Give our crawlers time to index more content</li>
                            </ul>
                        </div>
                    <% } else { %>

                        <div class="mt-3">
                            <% results.forEach(site => { %>
                                <article class="result-item">
                                    <div class="result-url-container">
                                        <% if (site.favicon) { %>
                                            <img src="<%= site.favicon %>" alt="" class="favicon-img" onerror="this.style.display='none'">
                                        <% } else { %>
                                            <div class="favicon-img d-flex align-items-center justify-content-center">
                                                <i class="bi bi-globe2" style="font-size: 9px; color: var(--text-muted);"></i>
                                            </div>
                                        <% } %>
                                        <a href="<%= site.url %>" class="result-url">
                                            <%= site.url.replace(/^https?:\/\//, '').replace(/\/$/, '') %>
                                        </a>
                                    </div>
                                    
                                    <a href="<%= site.url %>" class="result-title">
                                        <%= site.title %>
                                    </a>
                                    
                                    <p class="result-desc">
                                        <% if (site.description) { %>
                                            <%= site.description %>
                                        <% } else { %>
                                            <%= site.content.substring(0, 180) %>...
                                        <% } %>
                                    </p>
                                </article>
                            <% }) %>
                        </div>

                        <nav class="njin-pagination mt-4" aria-label="Search results pagination">
                            <% if (currentPage > 1) { %>
                                <a href="/search?q=<%= encodeURIComponent(query) %>&page=<%= currentPage - 1 %>" class="page-btn" aria-label="Go to previous page">
                                    <i class="bi bi-chevron-left"></i>
                                    <span class="d-none d-sm-inline">Previous</span>
                                </a>
                            <% } %>
                            
                            <span class="page-indicator" aria-current="page">Page <%= currentPage %> of <%= totalPages %></span>

                            <% if (currentPage < totalPages) { %>
                                <a href="/search?q=<%= encodeURIComponent(query) %>&page=<%= currentPage + 1 %>" class="page-btn" aria-label="Go to next page">
                                    <span class="d-none d-sm-inline">Next</span>
                                    <i class="bi bi-chevron-right"></i>
                                </a>
                            <% } %>
                        </nav>

                    <% } %> 
                </div>
            </div>

        <% } %> 
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
